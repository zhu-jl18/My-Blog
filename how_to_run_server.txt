To start the blog server, please run the following command in your terminal:

`hexo server`

Once the server is running, you can view the blog at this URL: http://localhost:4000/

To stop the server, press `Ctrl+C` in the terminal where you ran the `hexo server` command.